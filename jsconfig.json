{
  "compilerOptions": {
    "target": "ES2021",
    "module": "commonjs",
    "checkJs": false,
    "noEmit": true,
    "lib": ["ES2021", "DOM"]
  },
  "include": ["build.js", "tools/**/*.js", "src/js/**/*.js"],
  "exclude": ["node_modules"],
  "//": [
    "src/js/*.js are NOT modules. They are concatenated into one shared <script> scope by",
    "build.js, so cross-file references are intentional and there are no imports. Editors",
    "will still flag them as undefined — that is expected, not a bug. checkJs is off so the",
    "noise stays out of the way."
  ]
}
